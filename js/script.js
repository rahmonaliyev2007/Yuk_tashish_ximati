function toggleMenu() {
    const navLinks = document.getElementById("navLinks");
    navLinks.classList.toggle("active");
}
function changeLanguage(language) {
    alert("Til o'zgartirildi: " + language);
    
}